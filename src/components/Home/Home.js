import {Header} from "../Header/Header";
import {MovieList} from "../MoviesList/MovieList";

const Home = () => {
    return (
        <div>
            <Header/>
            <MovieList/>
        </div>
    );
};

export {Home};